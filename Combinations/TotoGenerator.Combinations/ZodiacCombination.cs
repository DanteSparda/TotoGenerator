namespace TotoGenerator.Combinations
{
    public class ZodiacCombination : Combination
    {
        public ZodiacCombination(int minValue, int maxValue, int totalNumbers)
            : base(minValue, maxValue, totalNumbers)
        {
        }        
    }
}
